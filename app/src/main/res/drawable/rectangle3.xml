<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="#f2f2f2" />

    <!-- Border properties -->
    <stroke
        android:width="2dp"
        android:color="#000000" /> <!-- Default black border -->

    <!-- Corner radius -->
    <corners
        android:radius="5dp" /> <!-- Rounded corners, customize radius -->

    <!-- Padding inside the rectangle -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp" />

    <!-- Gradient (optional, remove <solid> above if using this) -->
    <!-- Uncomment this section to enable gradient -->
    <!--
    <gradient
        android:angle="45"
        android:startColor="#FF0000"
        android:endColor="#00FF00"
        android:type="linear" />
    -->
</shape>
